name: mypy

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [ "3.9" ]
    name: MyPy
    steps:
    - uses: actions/checkout@v1
    - name: Install Dependencies and run MyPy
      run: |
        python -m venv venv
        source ./venv/bin/activate
        pip install poetry
        pip install --upgrade pip
        ls -la
        poetry env use 3.9
        ls -la
        cd backend/app;ls -la
        
        poetry install
        ../../venv/bin/mypy ./
