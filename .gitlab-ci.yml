stages:
    - test


Mypy:
    stage: test
    image: tiangolo/uvicorn-gunicorn-fastapi:python3.9
    script:
        - pip install mypy
        - mypy backend/app/app
    after_script:
        - ./docs/get-updated-badge-mypy.sh
    artifacts:
        paths:
            - badges-mypy.json
        when: always
        expire_in: 4 weeks
